# LLM providers
# Set at least one of these API keys.
COHERE_API_KEY=your_cohere_api_key
GEMINI_API_KEY=your_gemini_api_key

# Provider selection (optional)
# - LLM_PROVIDER sets the default provider for both router + agent
# - ROUTER_PROVIDER overrides for routing only
# - AGENT_PROVIDER overrides for graph agent only
LLM_PROVIDER=gemini
ROUTER_PROVIDER=gemini
AGENT_PROVIDER=cohere

# Model selection (optional)
# - GEMINI_MODEL applies to Gemini provider
# - COHERE_MODEL applies to Cohere provider
# - AGENT_MODEL overrides the agent model only
GEMINI_MODEL=gemini-3-flash-preview
COHERE_MODEL=command-a-03-2025
AGENT_MODEL=command-a-03-2025

# Router mode: rule | llm (optional; defaults to llm if any provider key is set)
ROUTER_MODE=llm
